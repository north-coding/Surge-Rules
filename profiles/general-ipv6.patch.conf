# Add these lines to the provider's existing [General] section.
# Do not replace Nexitally's block-quic, hijack-dns, skip-proxy, DNS,
# tun-excluded-routes, encrypted-dns-server or use-local-host-item-for-proxy settings.
ipv6 = true
ipv6-vif = auto
